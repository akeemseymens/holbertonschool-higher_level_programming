#!/usr/bin/python3
"""
validates if the object is an instance of specific class.

"""


def is_same_class(obj, a_class):
    """Check if an object is exactly an instance of the specific class."""
    return isinstance(obj, a_class):
